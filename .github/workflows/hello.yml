name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello World"

      - name: Print My Name
        run: echo "Hello Sudarshan"

      - name: Finish
        run: echo "GitHub Actions completed successfully!"
